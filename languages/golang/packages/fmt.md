```
fmt.Println("hello world")
// v is for any value in natural format
fmt.Printf("%d, %f, %t, %c, %v", 3, 3.4, true, 'a', 3) 
fmt.Sprintf("%d", 123) // equivalent to stconv.Itoa(123)
fmt.Fprintf(os.Stderr, "fetch: reading %s:%v", url, err)
fmt.Fprintf(&buf, %)
```